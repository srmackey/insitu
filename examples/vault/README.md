# Example vault (fictional)

This vault is sample data for tests and docs. It is not a real user vault.

The operator is invented. The project (River Ledger) is invented. The `clerk` role is a fictional pack so tests can exercise role expansion. Do not treat any of this as anyone's personal standing guidance.

Copy the shape, not the content, when you create `~/.insitu`.
